import styled from "styled-components";

const VentasStyles = styled.div`
.inputs-ventas{
  display: flex;
  flex-direction: inherit;
  left: 50%;
  transform: translate(10%, 150%);
}
`;
export default VentasStyles;


